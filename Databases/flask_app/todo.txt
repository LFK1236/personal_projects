Count number of entries owned in a series.

Buttons to filter based on user-defined criteria.

Allow user to optionally input language when creating volume.
Change default language.

Edit volume information.
Edit author information.
Edit information for entire series.

Make table for series which has name as primary key, the volumes table then references that instead of the other way around.

Add "edition" information for series.